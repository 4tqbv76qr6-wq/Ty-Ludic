/* ============================================================
   POPUP DE NIVEAU
   ============================================================ */
popupStart.addEventListener("click", () => {
    popup.classList.add("hidden");
    waitingForStart = false;
});
